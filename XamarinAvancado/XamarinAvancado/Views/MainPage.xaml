<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinAvancado.Views.MainPage"
             Title="{Binding Title}">

    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
        <Label Text="Pesquisar uma cidade"/>
        <Entry Text="{Binding CityName}"/>
        <Button Text="Pesquisar" Command="{Binding GoToAPI}"/>
        <Button Text="Second Page" Command="{Binding GoToSecond}"/>
        <Button Text="Show Message" Command="{Binding CallMsg}"/>
        <Button Text="Show Action Sheet" Command="{Binding DisplayActionSheet}"/>
    </StackLayout>

</ContentPage>